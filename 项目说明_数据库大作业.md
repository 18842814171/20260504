# 校园二手交易平台数据库系统 — 项目说明（数据库大作业）

> **【在线访问网址】**（请按实际部署情况修改后放在提交文档最开头）  
> - 公网可访问地址（如云服务器、内网穿透等）：`https://你的域名或IP:端口/`  
> - 本机/答辩演示本地地址示例：`http://127.0.0.1:8001/` 或 `http://本机IP:8001/`  
> - 若作业要求“无需运行环境即可访问的网址”，请说明该地址由您部署在公网后提供，并附访问时段或测试账号说明。

---

## 一、从运行代码到获得最终网址的具体步骤

下面以 **Linux 环境** 为例（与仓库内 `readme.md` 一致）；若使用 Windows + VS2022，请改用 CMake 生成工程后运行 `campus_trade_web`，浏览器访问 `http://127.0.0.1:8080` 或您配置的端口。

### 1. 准备 MySQL 数据库

1. 安装并启动 **MySQL 8.x**（或兼容版本），确保服务监听本机或指定主机（默认端口 `3306`）。  
2. 使用 root 或具有建库权限的账号，执行项目根目录下的 **`schema.sql`**，完成建库、建表、主键、外键、检查约束、初始数据与视图的创建。  
3. 在 MySQL 中确认数据库名与作业程序一致，默认可为 **`campus_trade`**（以 `schema.sql` 中 `CREATE DATABASE` 为准）。

### 2. 配置程序连接数据库的环境变量

本项目的 Web 服务通过 C++ 的 MySQL 客户端连接数据库。若与默认不一致，请在运行前设置（可在 shell 中 `export`，或写入启动脚本）：

| 变量名 | 含义 | 未设置时的默认行为（见 `HttpServer.cpp`） |
|--------|------|---------------------------------------------|
| `CAMPUS_DB_HOST` | 数据库主机 | `localhost` |
| `CAMPUS_DB_USER` | 用户名 | `root` |
| `CAMPUS_DB_PASS` | 密码 | `123456` |
| `CAMPUS_DB_NAME` | 数据库名 | `campus_trade` |
| `CAMPUS_DB_PORT` | 端口 | `3306` |

请根据您本机 MySQL 实际账号修改，避免因连接失败导致页面无法读库。

### 3. 编译 Web 服务程序

在项目根目录 `/home/linda/4.13`（或您的解压路径）执行：

```bash
cd /home/linda/4.13
make
```

成功后在 `build/` 目录下生成可执行文件 **`campus_trade_web`**（具体以 Makefile 规则为准）。若使用 CMake/VS，则编译目标名为 `campus_trade_web`，同理运行该可执行文件。

### 4. 启动 HTTP 服务并确定监听地址与端口

1. 可选：设置 HTTP 监听端口（默认 **8001**，见 `http_main.cpp`）以及绑定提示信息：

   ```bash
   export VM_HOST=0.0.0.0
   export VM_PORT=8001
   ```

   说明：`VM_HOST` 仅用于启动时在控制台打印提示；程序内部对 TCP 的绑定为监听所有接口（`0.0.0.0`），因此同一局域网内其他设备可通过 **本机局域网 IP + 端口** 访问。

2. 启动服务：

   ```bash
   ./build/campus_trade_web
   ```

3. 终端出现类似 `HTTP server started: http://127.0.0.1:8001` 即表示 Web 层已就绪。

### 5. 在浏览器中获得“最终网址”

- **本机访问**：在浏览器地址栏输入  
  `http://127.0.0.1:8001/login`  
  先完成登录（未登录会重定向到登录页）。登录成功后即可访问首页、商品、用户、订单、查询等路径。  
- **局域网其他机器访问**：将 `127.0.0.1` 换成本机在局域网中的 IP（如 `192.168.x.x`），端口与 `VM_PORT` 一致。  
- **作业要求的“公网最终网址”**：需在云主机或具备公网 IP 的机器上重复上述数据库部署与程序运行步骤，并在云平台 **安全组/防火墙** 中放行 HTTP 端口；最终提交的网址形如 `http://公网IP:端口/` 或绑定域名后的 `https://域名/`。

### 6. 建议的自检清单（避免“能编译但打不开页”）

- MySQL 已导入 `schema.sql`，且应用使用的账号能 `SELECT/INSERT/UPDATE/DELETE` 业务表。  
- 环境变量中的数据库密码、端口与真实 MySQL 一致。  
- 防火墙未拦截 Web 端口；云服务器已放行入站规则。  
- 浏览器使用 **http** 与正确端口，首次进入一般为 **`/login`**。

---

## 二、各类作业任务的运行结果简要说明（对应题目一至七）

以下说明便于撰写报告中的“截图 + 简要说明”：**界面路径** + **您应看到的现象** + **与数据库的对应关系**。

### （一）数据库定义

**运行/验证方式**：在 MySQL 中执行 `schema.sql`，或用客户端查看库 `campus_trade` 下的表结构。

**结果说明**：成功创建数据库 **`campus_trade`**，以及三张表 **`user`、`item`、`orders`**；各表具备主键；`item` 与 `orders` 含非空、检查约束（如 `price`、`status` 取值），`item.seller_id`、`orders.item_id`、`orders.buyer_id` 等与 `user`、`item` 的外键关系符合题目要求。截图建议：`SHOW CREATE TABLE` 或 ER 图旁注字段含义。

### （二）数据操作

**运行方式**：使用普通用户账号登录 Web → 进入 **商品** 相关页面（“我的商品”侧新增、删除未售出商品；在商品详情或列表触发修改价格等，以您页面实际表单为准）。

**结果说明**：

- **插入自定义商品**：提交后列表或提示中出现新 `item_id`，数据库 `item` 表增加一行，`status` 为未售出。  
- **修改价格**：刷新后表格中该商品价格更新，`item` 表对应行 `price` 变化。  
- **删除未售出商品**：仅当该商品 `status=0` 且属于当前卖家时可删除成功；刷新后该行消失；已售出商品不应被误删（程序与约束共同保证）。

### （三）基本查询

**运行方式**：浏览器打开 **`/queries`**（查询页），使用筛选条件：未售出状态、价格区间、类别（如生活类对应 `life`）、卖家 `u001` 等。

**结果说明**：页面以表格列出满足条件的商品，与 `SELECT ... FROM item WHERE ...` 一致。例如：仅选“未售出”则只看到 `status=0` 的行；价格下限填 `30` 则等价于“价格大于 30”类查询；类别选“生活”等则过滤 `category`；卖家填 `u001` 则列出该用户发布的商品。

### （四）连接查询

**运行方式**：结合 **商品详情**（`item` 与 `user` 连接展示卖家姓名与电话）、**订单页**（订单表数据）、以及必要时在 MySQL 中执行作业要求的 JOIN SQL（如已售商品与买家姓名等）。

**结果说明**：详情页应能看到卖家信息与商品信息的联合展示；订单列表能看到每笔订单的 `item_id`、`buyer_id`、`order_date` 等。若题目要求的三条连接查询部分在页面上以固定查询按钮展示，截图对应结果表即可；否则可在说明中附 SQL 执行结果截图，并注明与业务页面展示的数据一致。

### （五）聚合与分组

**运行方式**：若 Web 未单独做统计页，可在 **MySQL 客户端** 执行：商品总数 `COUNT(*)`、按类分组计数 `GROUP BY category`、平均价格 `AVG(price)`、发布数量最多的用户（对用户分组 `COUNT` 后排序取 Top）。  

**结果说明**：截图中应出现聚合结果数字或分组表；文字说明中写清各指标含义及与 `item`/`user` 表数据一致。

### （六）视图

**运行方式**：`schema.sql` 已创建视图（如 **`v_sold_items_with_buyer`**、**`v_unsold_items`** 等）；在 MySQL 中 `SELECT * FROM v_...` 验证。

**结果说明**：已售商品视图含商品名与买家 ID 等字段；未售商品视图仅含未售出商品相关列。截图建议：视图定义 + 查询结果若干行。

### （七）简单业务逻辑 —— 购买商品

**运行方式**：普通用户登录 → **`/orders`** 页面使用“购买商品”表单，填写新的 `order_id` 与要买的 `item_id`（商品须未售出）。

**结果说明**：成功后 **`orders` 表新增一行**，对应 **`item.status` 变为已售出（1）**；同一商品再次购买时，程序在事务中检测到已售并回滚，不会重复插入订单。这与代码中 **`START TRANSACTION` + `SELECT ... FOR UPDATE` + `INSERT` + `UPDATE` + `COMMIT/ROLLBACK`** 的逻辑一致。

---

## 三、简答：（八）安全性

### 1. 如何防止普通用户删除数据（越权删除）

可从 **应用层** 与 **数据库层** 两方面说明（与本项目实现要点对应）：

**（1）应用层 —— 身份与角色**

- 系统通过登录后下发的 **`session_id` Cookie**（见登录成功后的 `Set-Cookie`）区分会话；会话中记录 **角色**（管理员 `admin` 与普通用户 `user`）及 **用户 ID**。  
- **删除未售出商品**的接口仅允许 **普通用户** 调用；管理员在商品列表侧为 **只读浏览**，不提供随意删除任意商品的入口，从而避免“误操作或恶意批量删库式删除”。  
- 删除语句在服务端拼接时强制 **`seller_id = 当前登录用户的 user_id`**，且 **`status = 0`**（仅未售出）。因此普通用户 **既不能删除他人发布的商品**，也 **不能删除已售出商品**（与作业一致性规则一致）。普通用户只能删除“自己的、未售出的”记录，从权限上收窄删除范围。

**（2）数据库层 —— 约束与外键**

- 通过 **主键、外键、唯一约束**（如 `orders.item_id` 唯一）保证删除或变更不会破坏引用完整性；若对 `user`/`item` 配置删除策略为 **RESTRICT**，则可防止误删仍被订单引用的商品或用户（具体以您在库中设置的 `ON DELETE` 为准）。

**（3）输入安全**

- 对用户输入的字符串在拼入 SQL 前进行 **转义**（如 `sqlEscape`），降低 **SQL 注入** 风险，避免攻击者通过表单构造语句删除整张表。

综上：**防止普通用户“乱删数据”**依赖 **角色校验 + 按卖家限定 DELETE 条件 + 库级完整性约束 + 输入转义** 多层配合。

### 2. 如何限制用户只能查询数据

题目可从 **数据库权限** 与 **应用功能** 两个层次作答：

**（1）数据库层 —— 只读账号**

- 在 MySQL 中建专门账号，例如只对 `campus_trade` 授予 **`SELECT`**：  
  `GRANT SELECT ON campus_trade.* TO 'report_user'@'%' IDENTIFIED BY '...';`  
  报表工具、对外只读 API 使用该账号则 **无法在数据库层面执行 INSERT/UPDATE/DELETE**，从根上限制为“只能查”。

**（2）应用层 —— 功能隔离**

- 对仅需浏览数据的角色，Web 端 **只提供 GET 查询类页面**，不提供修改类表单；后台连接若使用只读账号，则即使程序有误也难以写入。  
- 本项目中 **管理员** 可查看完整用户列表与订单信息，但订单页对管理员提示为查看性质；**普通用户** 在订单页只能看到 **自己的订单**（`WHERE buyer_id = 当前用户`），属于 **数据范围上的查询限制**，避免横向越权看到他人订单。

实际报告中可将 **“库级只读授权”** 与 **“Session + SQL 条件过滤”** 结合写清：**既限制操作类型（只读账号），又限制可见范围（业务 WHERE）**。

---

## 四、简答：（九）并发与恢复

### 1. 两个用户同时购买同一未售商品，问题出在哪里

一次“购买”在逻辑上至少要经历三步：先读 `item` 表确认该商品仍是未售（`status=0`），再向 `orders` 插入一条新订单，最后把该商品的 `status` 更新为已售。两个买家几乎同时提交时，若数据库端没有任何互斥手段，两个 HTTP 请求各自用一条连接去执行查询，时间线很容易交错成：请求 A 在 T1 时刻执行普通的 `SELECT`，读到 `status=0`；请求 B 在 T2 时刻同样执行 `SELECT`，读到的仍是 `status=0`，因为此时还没有任何一方完成更新。随后 A 插入订单并执行 `UPDATE`；B 也认为自己“有资格购买”，同样去插入订单并更新——从业务上讲这件商品只能卖一次，系统里却出现了两条针对同一 `item_id` 的成单企图，这就是典型的“先读后写”竞态：检查和写入之间开了一个窗口，第二个事务仍能穿过这个窗口。

后果需要分开说。对业务规则而言，直接违反“每商品最多交易一次”的约束；若应用和库都放得很松，甚至可能出现两条有效订单。对本作业的表结构而言，`orders.item_id` 上有唯一约束，第二笔 `INSERT` 往往会以重复键失败告终，表面上“没有双单”，但第二个用户看到的常常是数据库层面的错误码，而不是清晰的“商品已被他人买下”。更麻烦的是没有事务包裹时的顺序问题：例如先插入订单、进程在更新 `status` 之前崩溃，就会出现订单表里多了一行而商品仍显示未售；或者只更新了状态而订单没写入——这些半成品状态都不符合我们期望的一致性叙述。

### 2. 解决办法：事务把三步绑成原子单元，用 `FOR UPDATE` 锁住商品行

本项目在服务端购买路径上使用 MySQL InnoDB 提供的能力，把“读状态—插订单—改状态”放进同一个事务里，并在读商品时用行级锁把并发强行排成一队。具体顺序是：先 `START TRANSACTION`；立刻对目标商品执行 `SELECT status FROM item WHERE item_id = ? FOR UPDATE`。`FOR UPDATE` 的含义是：在当前事务结束之前，其他事务若也想对同一行加锁或修改，就必须等待。这样就不会出现两个事务在毫秒内都读到 `status=0` 再一起往下写的情况——先获得行锁的事务读完、插入 `orders`、执行 `UPDATE item SET status = 1`，最后 `COMMIT` 释放锁；后获得锁的事务再执行同样的 `SELECT ... FOR UPDATE` 时，读到的已经是已售，应用代码据此 `ROLLBACK`，不再插入第二条订单。

若 `INSERT` 或 `UPDATE` 中途失败，同一事务内调用 `ROLLBACK`，订单插入和状态修改都不会单独留下，避免“只有订单没有改状态”或相反。表上的 `item_id` 唯一约束仍然保留，作为最后一道保险；正常设计上应依赖锁把冲突挡在业务层，而不是依赖报错让用户反复尝试。

### 3. 系统崩溃后，订单数据如何恢复

先要区分崩溃发生在哪一层。如果只是运行 `campus_trade_web` 的进程异常退出，而 MySQL 服务一直在运行，那么只要购买流程在数据库里已经执行到 `COMMIT`，订单行和 `item.status` 的修改就已经交给 InnoDB 持久化，重启 Web 程序后查库，数据仍在。这与“整个库跟着机器断电”不是同一种情况。

当 MySQL 进程或整机异常终止时，InnoDB 在下次启动时会自动做崩溃恢复。平时对数据页的修改会先按顺序记到 redo log（重做日志）再异步刷到表空间；未提交事务的修改则可以通过 undo log（撤销日志）在恢复阶段撤掉。恢复的结果是：已经提交的事务，其效果会通过 redo 重放保证不丢；尚未提交的事务，其半成品修改会被撤销，库回到一个内部一致的状态，不会出现“订单写进了一半、商品状态却永远停在中间值”这种脏数据长期留在磁盘上的情况。

若遇到磁盘损坏、误删库等灾难，单靠 redo/undo 不够，需要运维上的备份策略：定期全量备份，并开启 binlog 记录提交后的变更序列。需要把库恢复到某一历史时刻时，用一份全备加上自备份点起的 binlog 顺序重放，即可做到时间点恢复（PITR）；云数据库托管产品里的“按时间点还原”本质上也是备份与日志的组合。写报告时把“引擎自动恢复”与“备份+binlog 应对介质故障”分开写，比笼统说“用日志恢复”更贴近实际。

---

## 五、文档与视频提交时的对应关系（便于自查）

| 作业要求条目 | 建议在说明文档 / 视频中的体现 |
|--------------|-------------------------------|
| 网址在最开头 | 本文第一节上方的【在线访问网址】 |
| 运行步骤 | 本文“从运行代码到获得最终网址的具体步骤” |
| 网页与各任务截图 | 本文“各类作业任务的运行结果简要说明” + 实际截图 |
| 简答第八、九部分 | 本文第三节、第四节 |
| 视频全流程 | 从浏览器输入网址 → 登录 → 导航各页 → 完成查询/购买/维护数据等操作 |

---

*（完）您可将本文内容复制到 Word 按学院格式排版，并替换网址、截图占位及与您部署环境不一致的默认账号信息。*
